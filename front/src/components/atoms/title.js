import styled from 'styled-components';

const Title = styled.label`
    font-family: Roboto;
    font-weight: 900;
    font-size: 16px;
    line-height: 24px;
    color: #172847;
`;

export default Title;